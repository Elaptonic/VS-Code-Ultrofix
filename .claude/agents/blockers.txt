# Blockers & Task Dependencies

## Current Status
Task 0.1 (session expiry / 401 handling) is complete on both backend and
frontend. Two pre-existing issues were found during verification (2026-06-15)
and have now been resolved:

1. URBAN-APP-TYPECHECK - RESOLVED (2026-06-15) - `pnpm --filter
   @workspace/urban-app run typecheck` now passes (0 errors). Fixes:
   - app/_layout.tsx:68 - cast `useSegments()` to `string[]` (no generated
     typed-routes file in this checkout).
   - app/booking/[id].tsx:64,68,165, app/category/[id].tsx:29,
     app/service/[id].tsx:36,40 - "UseQueryOptions missing queryKey" was
     caused by Orval not detecting @tanstack/react-query v5 (the
     `lib/api-client-react/package.json` dependency uses a pnpm `catalog:`
     version string, which Orval can't version-compare). Fixed by setting
     `override.query.version = 5` in `lib/api-spec/orval.config.ts` and
     re-running `pnpm --filter @workspace/api-spec run codegen` - generated
     `query?:` options are now `Partial<UseQueryOptions<...>>`.
   - app/booking/[id].tsx:173 - "BookingStatus vs 'accepted' no overlap" was
     caused by `openapi.yaml`'s `Booking.status` / `UpdateBookingBody.status`
     enum (`[upcoming, completed, cancelled]`) being out of sync with the DB
     enum in `lib/db/src/schema/bookings.ts`
     (`[pending, accepted, in_progress, completed, cancelled]`). Fixed the
     enum in `openapi.yaml` to match the DB schema and regenerated.
   - hooks/useProviderTracking.ts:57 - captured `activeJob` in a local
     `const job` before the async closure (TS can't narrow a captured
     parameter across an `await`).

2. API-SERVER-VITEST-WINDOWS - RESOLVED (2026-06-15) - added
   `.github/workflows/test.yml` (GitHub Actions, ubuntu-latest) running
   `pnpm --filter @workspace/api-server run test` and
   `pnpm --filter @workspace/db run test` on Linux, where the
   pnpm-workspace.yaml `overrides` (linux-x64-only native binaries) are
   correct. Tests still cannot run locally on this Windows machine - use CI
   or a Linux/WSL environment for local Vitest runs.

## Frontend Task Dependencies
- Any screen/hook consuming a new or changed API endpoint depends on:
  1. Schema change in lib/db/src/schema/ (Backend Agent)
  2. lib/api-spec/openapi.yaml updated + `pnpm --filter @workspace/api-spec run codegen` run
  3. lib/api-zod and lib/api-client-react regenerated and typechecked

## Backend Task Dependencies
- Any route change that affects the API contract depends on:
  1. lib/api-spec/openapi.yaml updated to match the new request/response shape
  2. Codegen run so lib/api-zod stays in sync (Frontend Agent consumes this)

## Unblocked Tasks
- PAYMENT-MODE-CARD - UNBLOCKED - frontend picker (PaymentModeCard.tsx) added to
  existing app/booking/[id].tsx payment step; local-state only, no backend
  dependency; urban-app typecheck PASS.
- BACKEND-APPTS-TSERRORS - UNBLOCKED - reported TS2349 (app.ts:12) and TS7006
  (app.ts:15, app.ts:22) do not reproduce; api-server typecheck PASS (0
  errors) with file unchanged. No fix applied.

## How to Update
- Add one line per task: `<Task ID> - <BLOCKED|UNBLOCKED|WAITING> - <reason>`
- Analyst updates this file after each review (see analyst.md section 6)
- Frontend/Backend agents check this file before starting a task (pre-execution checklist)
